{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }}{{ movie.title }}
{% endblock %}

{% block titre %}
    {{ movie.title }}
{% endblock %}

{% block body %}


    <div class="card-body row justify-content-md-around border">

        <div class="col-4">
            <img src="{{ asset('uploads/') }}{{ movie.cover }}" height="500">
        </div>

        <div class="col-6">
            <h3>{{ movie.title }}</h3>

            <p>{{ movie.releaseDate | date('d/m/Y') }}</p>
            <p>Réalisateur : {{ movie.director }}</p>
            <p>Distribution : {% for actor in movie.actors %}
                    {{ actor.firstname }} {{ actor.lastname }}
                {% endfor %}
            </p>
            <p>{{ movie.resume }}s</p>
            {% if affich==false %}
                <a href="{{ path('formReview', {id:movie.id ,param:true}) }}" class="btn btn-warning">Ajouter un
                    avis</a>
            {% else %}

                <form method="post" action="{{ path('detailMovie', {'id': movie.id}) }}" >
                    <div class="form-group">
                <label>Commentaire</label>
                <input class="form-control" type="textarea" name="review" id="review">
                </div>
                <div class="form-group">
                    <label for="note">Note</label>
                    <select class="form-control" name="rating" id="rating">
                        <option value="1">1</option>
                        <option value="2">2</option>
                        <option value="3">3</option>
                        <option value="4">4</option>
                        <option value="5">5</option>
                        <option value="6">6</option>
                        <option value="7">7</option>
                        <option value="8">8</option>
                        <option value="9">9</option>
                        <option value="10">10</option>
                    </select>
                </div>

                <button class="btn btn-primary" type="submit">Envoyer!</button>
                </form>
            {% endif %}

        </div>

    </div>

    <div class="card-body">
        <h4 class="card-title">NOTE - DATE </h4>
        <h6 class="card-subtitle mb-2 text-muted">PSEUDO</h6>
        <p class="card-text">COMMENTAIRE LAISSE PAR L'UTILISATEUR</p>

    </div>

    </div>

{% endblock %}